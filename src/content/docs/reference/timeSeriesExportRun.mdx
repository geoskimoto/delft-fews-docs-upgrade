---
title: Export module file
description: Reference for the timeSeriesExportRun config file — write FEWS series out to files/services. Field reference auto-generated from the schema.
sidebar:
  order: 12
---

import { Aside } from '@astrojs/starlight/components';
import FieldReference from '../../../components/FieldReference.astro';
import data from '../../../data/schema/timeSeriesExportRun.json';

The schema-companion to the [Export data](/tasks/export/) task guide. Export is
the mirror of [import](/reference/timeseriesimportrun/); this page is the
generated field list.

## At a glance

<div class="field-list">

**Purpose**
: Reads internal series and writes them out to a file/service/database, renaming
via ID map on the way out ([hop ⑤](/concepts/data-flow/), reverse boundary).

**Folder**
: `Config/ModuleConfigFiles/`

**Root element**
: `<timeSeriesExportRun>` (**no** `version` attribute — a module-run file)

**Depends on**
: An [`idMap`](/reference/idmap/) (applied internal → external) and the series it
reads.

</div>

## Minimal example

```xml title="Config/ModuleConfigFiles/ExportDischarge 1.00 default.xml"
<?xml version="1.0" encoding="UTF-8"?>
<timeSeriesExportRun xmlns="http://www.wldelft.nl/fews">
  <export>
    <general>
      <exportType>generalCsv</exportType>
      <folder>$EXPORT_FOLDER$/discharge</folder>
      <exportFileName>
        <name>discharge.csv</name>
      </exportFileName>
      <idMapId>IdExportObservations</idMapId>
      <exportTimeZone>
        <timeZoneName>GMT</timeZoneName>
      </exportTimeZone>
    </general>
    <timeSeriesSet>
      <moduleInstanceId>ImportObservations</moduleInstanceId>
      <valueType>scalar</valueType>
      <parameterId>Q.obs</parameterId>
      <locationId>RiverGauge_01</locationId>
      <timeSeriesType>external historical</timeSeriesType>
      <timeStep unit="hour" multiplier="1"/>
      <readWriteMode>read only</readWriteMode>
    </timeSeriesSet>
  </export>
</timeSeriesExportRun>
```

## Field reference

<FieldReference data={data} />

## See also

- Task guide: [Export data](/tasks/export/)
- Mirror: [Import module file](/reference/timeseriesimportrun/)
- Schema: [`timeSeriesExportRun.xsd`]({data.schemaUrl})
