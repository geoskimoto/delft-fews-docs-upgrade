---
title: Display groups file
description: Reference for the DisplayGroups config file — predefined chart and table views. Field reference auto-generated from the schema.
sidebar:
  order: 7
---

import { Aside } from '@astrojs/starlight/components';
import FieldReference from '../../../components/FieldReference.astro';
import data from '../../../data/schema/displayGroups.json';

The schema-companion to the [Filters & display groups](/tasks/filters-displays/)
task guide. That guide explains filters vs. displays and the plot structure; this
page is the generated field list.

## At a glance

<div class="field-list">

**Purpose**
: Bundles related series into predefined chart/table views — *how a chosen thing
is drawn* ([hop ⑤](/concepts/data-flow/)).

**Folder**
: `Config/DisplayConfigFiles/`

**Root element**
: `<displayGroups>` (`version="1.0"`, optional)

**Depends on**
: The [locations](/reference/locations/), [parameters](/reference/parameters/),
and stored series each `timeSeriesSet` queries.

</div>

## Minimal example

```xml title="Config/DisplayConfigFiles/DisplayGroups 1.00 default.xml"
<?xml version="1.0" encoding="UTF-8"?>
<displayGroups xmlns="http://www.wldelft.nl/fews" version="1.0">
  <plot id="DischargePlot">
    <subplot>
      <line>
        <color>0000FF</color>
        <lineStyle>solid</lineStyle>
        <timeSeriesSet>
          <moduleInstanceId>ImportObservations</moduleInstanceId>
          <valueType>scalar</valueType>
          <parameterId>Q.obs</parameterId>
          <locationId>RiverGauge_01</locationId>
          <timeSeriesType>external historical</timeSeriesType>
          <timeStep unit="hour" multiplier="1"/>
          <readWriteMode>read only</readWriteMode>
        </timeSeriesSet>
      </line>
    </subplot>
  </plot>
  <displayGroup name="Discharge gauges">
    <display name="River Gauge 01">
      <locationId>RiverGauge_01</locationId>
      <plotId>DischargePlot</plotId>
    </display>
  </displayGroup>
</displayGroups>
```

## Field reference

<Aside type="note">
This is a large surface — display groups expose extensive charting options
(axes, markers, subplots, colours). The full graph is below; most configs use a
small subset (a `plot` with one `subplot` and a few `line`s).
</Aside>

<FieldReference data={data} />

## See also

- Task guide: [Filters & display groups](/tasks/filters-displays/)
- Reference: [Filters file](/reference/filters/)
- Schema: [`displayGroups.xsd`]({data.schemaUrl})
